spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userdb
    username: root
    password: Tiger
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081   # user-service runs on 9082
